# Unified requirements for the vidplatform monorepo.
# Covers apps/api (FastAPI), apps/worker (Celery), apps/modal_app (GPU workers).
# Pinned to known-good versions. Install:
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt

# ── Web framework + async stack ─────────────────────────────
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.35
sse-starlette>=2.1,<3.0
python-multipart>=0.0.12
httpx>=0.27,<0.29

# ── Pydantic + settings ─────────────────────────────────────
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# ── Database (async + sync) ─────────────────────────────────
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2.0
asyncpg>=0.30
psycopg[binary]>=3.2

# ── Redis (broker + SSE pubsub) ─────────────────────────────
redis>=5.2,<6.0

# ── Object storage ──────────────────────────────────────────
boto3>=1.35,<2.0

# ── Pipeline orchestration ──────────────────────────────────
celery[redis]>=5.4,<6.0

# ── Modal serverless GPU ────────────────────────────────────
modal>=0.66

# ── LLM (Gemini via Google AI Studio) ───────────────────────
google-genai>=0.3
langgraph>=0.2.50
langchain-core>=0.3

# ── Observability ───────────────────────────────────────────
structlog>=24.4

# ── Audio/video helpers used by FFmpeg compositor & subtitle
#    extraction. The Modal containers install heavyweight model
#    deps (torch, diffusers, ACE-Step, MuseTalk) at image-build
#    time on Modal — keep those out of this file.
pydub>=0.25
ffmpeg-python>=0.2
soundfile>=0.12
numpy>=1.26

# ── Dev / test ──────────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24
respx>=0.21
ruff>=0.7
